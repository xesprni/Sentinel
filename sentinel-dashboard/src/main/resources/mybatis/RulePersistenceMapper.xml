<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.csp.sentinel.dashboard.repository.mapper.RulePersistenceMapper">

    <insert id="insertOrUpdate">
        INSERT INTO sentinel_rule (app, rule_type, rule_data)
        VALUES (#{app}, #{ruleType}, #{ruleData})
        ON DUPLICATE KEY UPDATE rule_data = #{ruleData}
    </insert>

    <select id="getRuleData" resultType="string">
        SELECT rule_data
        FROM sentinel_rule
        WHERE app = #{app} AND rule_type = #{ruleType}
    </select>

    <select id="listAllRuleData" resultType="map">
        SELECT app AS app, rule_type AS ruleType, rule_data AS ruleData
        FROM sentinel_rule
    </select>

</mapper>
